# Kanban-Complete-Mover
Obsidian Plugin for Kanban, Main feature: move a card to a Complete lane (auto-created if missing) the moment its checkbox is checked with optional completion-date stamp on the same move.
